#version 330 core

// Ouput data
out vec4 fragColor;

in vec3 colorRes; // the input variable from the vertex shader (same name and same type)  

void main()
{
	// Output color = red 
	fragColor = vec4(colorRes, 1);

}